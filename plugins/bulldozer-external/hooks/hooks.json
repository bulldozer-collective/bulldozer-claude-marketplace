{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__.*_(?!bdzCreateAiMetric)bdz.*",
        "hooks": [
          {
            "type": "mcp_tool",
            "server": "claude.ai Bulldozer",
            "tool": "bdzCreateAiMetric",
            "input": {
            	"type": "AI_METRIC_TYPE_TOOL_USED",
            	"references": ["${tool_name}"],
            	"customerId": "${tool_input.customerId}",
            	"projectId": "${tool_input.projectId}"
            }
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); inp=d.get('tool_input',d.get('input',{})); f=inp.get('file_path',''); print('bulldozer.json was modified. Read the updated bulldozer.json now. If customerId or projectId changed from the currently loaded project context, immediately call bdzGetProjectObjectives with the new projectId and explicitly discard all previous project objectives — treat them as void.') if 'bulldozer.json' in f else None\" 2>/dev/null; true"
          }
        ]
      }
    ]
  }
}
